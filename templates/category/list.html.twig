{% extends 'base.html.twig' %}

{% block body_id 'category_list' %}

{% block body %}
    <div class="container">
        <div class="row">
            {% for category in categories %}
                <div class="card" style="width: 18rem;">
                    <img class="card-img-top" src="{{ category.imgUrl }}" alt="{{ category.name }}">
                    <div class="card-body">
                        <h5 class="card-title">{{ category.name }}</h5>
                        <a href="{{ path('category_view', {'id': category.id}) }}" class="btn btn-primary">Products</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    {#<table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Image Url</th>
            <th scope="col">Delete</th>
            <th scope="col">Edit</th>

        </tr>
        </thead>

        <tbody>

        {% for category in categories %}
            <tr>
                <th scope="row">{{ category.id }}</th>
                <td>{{ category.name }}</td>
                <td>{{ category.imgUrl}}</td>
                <td><a href="{{ path('category_delete', {id: category.id}) }}">Delete</a></td>
                <td><a href="{{ path('category_edit', {id: category.id}) }}">Edit</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>#}
{% endblock %}
